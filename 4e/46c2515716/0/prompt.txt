when running `entire enable` in a setup repo and everything is enabled, can we show which agent hooks are setup and also mention that to add more agents `entire setup` should be used

---

maybe "configure" is better then "setup" that way "help" would also make more sense since we could show "Configure Entire in current project" (but maybe we also use "current repository" both for enable and configure)

---

remove the "setup" alias (or was that still a thing before the changes in this branch)

---

remove init too

---

can we additional output enable shows when already enable to when we switch from disabled to enable too?

---

when running `configure` this is shown:

┃ Which agents are you using?
┃ Use space to select, enter to confirm. Already-enabled agents cannot be removed.

When something is already enable the question is a bit misleading or not as clear. Any other suggestions?

---

Let's do "enable more agents" instead of "add"

---

-  To get started with Entire CLI, run 'entire enable' to configure
-  your project's environment. For more information, visit:
+  To get started with Entire CLI, run 'entire configure' to set up
+  your repository. For more information, visit:

let's stick to "configure" and not set up