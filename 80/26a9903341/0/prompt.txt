we need to reset this? Run if [ -z "${ENTIRE_AUTH_TOKEN}" ]; then
**Notice:** ENTIRE_AUTH_TOKEN length=47 starts_with_ent=true sha256_prefix=036d5750c861
ENTIRE_API_BASE_URL=<default>
Token for [https://entire.io](https://entire.io/) is no longer valid.
Update or unset ENTIRE_AUTH_TOKEN to re-authenticate.
**Error:** ENTIRE_AUTH_TOKEN was found but rejected by Entire.
**Error:** Process completed with exit code 1.