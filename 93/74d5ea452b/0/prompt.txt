1. 10-second blocking on every git push when remote is down (checkpoint_remote.go:39-78)
  — isRemoteReachable() runs git ls-remote with a 10s timeout on every push. If the
  checkpoint remote is unreachable (network issues, misconfigured URL), this silently adds
  up to 10 seconds of latency before falling back. Consider removing the reachability check
   entirely and letting the push itself fail/recover, or caching the result.

yeah, let's do this, handle the failure. The issue is a bit that we then would add the remote the next push again since it's not set but `.entire/settings.json` does specify it, right?

---

but how would that work if I have this working with a local + in the same repo branch then and now the repo change it to remote (one of the engineers makes the PR, merges it) and now it's pulled local, at that point we should pickup locally that now a different remote should be used

---

ignore the content scenario, but what's the flow if the engineer pulls this, remote is updated but it's not accessible to him?

---

what's the warning message right now?

---

Let's make this message clearer, that if a remote is set, the push went to the remote, let's have a more explicit error message

---

don't ask to override locally, ask to get access to the repo

---

don't ask to override locally, ask to get access to the remote

---

don't ask to override locally, ask to get access to the remote